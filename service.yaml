apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  type: NodePort
  selector:
    app: newflashapp
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007
